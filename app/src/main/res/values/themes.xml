<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MediScanner_Firebase" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MediScanner_Firebase" parent="Base.Theme.MediScanner_Firebase" />

    <style name="roundedImageViewRounded">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <style name="CalendarViewTheme" parent="Theme.AppCompat">
        <!-- Hier kannst du verschiedene Eigenschaften für das Kalenderdesign festlegen -->
        <item name="android:colorPrimary">@color/colorPrimary</item>
        <item name="android:colorAccent">@color/colorAccent</item>
        <!-- Weitere Attribute können hinzugefügt werden -->
    </style>
</resources>